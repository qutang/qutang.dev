import MarkdownLayout from "../layouts/markdown";
import parseMarkdown from '../components/markdown';

export const meta = {
  type: "mdx",
  title: "Resources about data, algorithm, AI and research",
  date: "2018-06-23 16:14:53",
  markdownFile: 'resource.md',
  markdownContent: parseMarkdown('resource.md'),
  excerpt:
    "Collection of resources from internet about _data, algorithm, AI, and research."
};

export const layout = props => (
  <MarkdownLayout {...props} meta={meta}>
    {props.children}
  </MarkdownLayout>
);
