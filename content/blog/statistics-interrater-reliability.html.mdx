import parseMarkdown from '../../components/markdown';

export const meta = {
  type: "mdx",
  title: "评分者信度",
  date: "2017-06-23 16:14:53",
  series: "stats",
  markdownFile: 'statistics-interrater-reliability/index.md',
  markdownContent: parseMarkdown('statistics-interrater-reliability/index.md'),
  excerpt: "评分者间信度 (Interrater reliability) 就是用不同数据收集者之间一致性来度量数据可信度的一种概念。"
};

import MarkdownLayout from "../../layouts/markdown";

export const layout = props => (
  <MarkdownLayout {...props} meta={meta}>
    {props.children}
  </MarkdownLayout>
);


