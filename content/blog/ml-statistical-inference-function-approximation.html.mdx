import parseMarkdown from '../../components/markdown';

export const meta = {
  type: "mdx",
  title: "统计推断和函数估计",
  date: "2019-08-19T16:51:00",
  series: "ml",
  markdownFile: 'ml-statistical-inference-function-approximation.md',
  markdownContent: parseMarkdown('ml-statistical-inference-function-approximation.md'),
  excerpt:
    "从statistical model和function approximation两个角度看待机器学习算法的问题。"
};

import MarkdownLayout from "../../layouts/markdown";

export const layout = props => (
  <MarkdownLayout {...props} meta={meta}>
    {props.children}
  </MarkdownLayout>
);
