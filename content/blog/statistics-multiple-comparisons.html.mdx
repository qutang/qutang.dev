import parseMarkdown from "../../components/markdown";

export const meta = {
  type: "mdx",
  title: "多重比较",
  date: "2017-06-23 16:14:53",
  series: "stats",
  markdownFile: "statistics-multiple-comparisons/index.md",
  markdownContent: parseMarkdown(
    "statistics-multiple-comparisons/index.md",
  ),
  excerpt:
    "多重比较 (Multiple comparisons) 是指通过多个统计测试来做出推论的方式"
};

import MarkdownLayout from "../../layouts/markdown";

export const layout = props => (
  <MarkdownLayout {...props} meta={meta}>
    {props.children}
  </MarkdownLayout>
);
