import parseMarkdown from '../../components/markdown';

export const meta = {
  type: "mdx",
  title: "Automated smoking detection",
  date: "2016-08-04T00:00:00",
  markdownFile: 'automated-puffing-and-smoking-detection.md',
  markdownContent: parseMarkdown('automated-puffing-and-smoking-detection.md'),
  excerpt:
    "A compelementary introduction for a published paper about an Activity recognition algorithm based on Random Forest to automatically detect puffing and smoking behavior using wrist accelerometers."
};

import MarkdownLayout from "../../layouts/markdown";

export const layout = props => (
  <MarkdownLayout {...props} meta={meta}>
    {props.children}
  </MarkdownLayout>
);
